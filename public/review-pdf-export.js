const PDF_VERSION='20260815-pdf1';
let generating=false;
async function libs(){const [{default:html2canvas},{jsPDF}]=await Promise.all([import('https://esm.sh/html2canvas@1.4.1'),import('https://esm.sh/jspdf@2.5.2')]);return{html2canvas,jsPDF}}
function safeName(){const raw=(document.querySelector('#printPage .identity .name')?.textContent||'Employee').trim();return raw.replace(/[^a-z0-9 _-]/gi,'').replace(/\s+/g,'_')||'Employee'}
async function buildPdf(){if(generating)return null;generating=true;try{const pages=[...document.querySelectorAll('#printPage.review-summary-v6 .p')];if(pages.length!==2)throw new Error(`Expected 2 review pages, found ${pages.length}.`);const {html2canvas,jsPDF}=await libs();const pdf=new jsPDF({orientation:'portrait',unit:'pt',format:'letter',compress:true});for(let i=0;i<pages.length;i++){if(i)pdf.addPage('letter','portrait');const canvas=await html2canvas(pages[i],{scale:2,backgroundColor:'#ffffff',useCORS:true,logging:false,width:pages[i].scrollWidth,height:pages[i].scrollHeight,windowWidth:pages[i].scrollWidth,windowHeight:pages[i].scrollHeight});const img=canvas.toDataURL('image/jpeg',0.94);pdf.addImage(img,'JPEG',0,0,612,792,undefined,'FAST')}return pdf}finally{generating=false}}
async function downloadPdf(btn){const old=btn.textContent;btn.disabled=true;btn.textContent='Building PDF...';try{const pdf=await buildPdf();if(!pdf)return;pdf.save(`${safeName()}_CTOD_Review.pdf`)}catch(e){alert(`PDF could not be generated: ${e.message||e}`)}finally{btn.disabled=false;btn.textContent=old}}
async function printPdf(btn){const old=btn.textContent;btn.disabled=true;btn.textContent='Preparing Print...';try{const pdf=await buildPdf();if(!pdf)return;const blob=pdf.output('blob');const url=URL.createObjectURL(blob);const w=window.open(url,'_blank');if(!w){const a=document.createElement('a');a.href=url;a.download=`${safeName()}_CTOD_Review.pdf`;a.click();alert('CTOD created the two-page portrait PDF. Open the downloaded PDF and print it at 1 page per sheet.')}else{setTimeout(()=>{try{w.focus();w.print()}catch{}},1200);setTimeout(()=>URL.revokeObjectURL(url),120000)}}catch(e){alert(`PDF could not be prepared for printing: ${e.message||e}`)}finally{btn.disabled=false;btn.textContent=old}}
function install(){const host=document.querySelector('#printPage.review-summary-v6');if(!host||!host.querySelector('.p'))return;let actions=host.querySelector('.actions');if(!actions)return;if(actions.dataset.pdfExport===PDF_VERSION)return;actions.dataset.pdfExport=PDF_VERSION;actions.innerHTML=`<button id="downloadReviewPdf" class="btn primary">Download PDF</button><button id="printReviewPdf" class="btn primary">Print 2 Portrait Pages</button><button id="backReviewPdf" class="btn secondary">Back to Review</button>`;actions.querySelector('#downloadReviewPdf').onclick=e=>downloadPdf(e.currentTarget);actions.querySelector('#printReviewPdf').onclick=e=>printPdf(e.currentTarget);actions.querySelector('#backReviewPdf').onclick=()=>{host.style.display='none';host.innerHTML='';host.className='printpage';const app=document.querySelector('#app');if(app)app.hidden=false}}
new MutationObserver(()=>install()).observe(document.body,{subtree:true,childList:true});document.addEventListener('click',()=>setTimeout(install,0),true);install();